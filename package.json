{
  "name": "ytconvguide",
  "version": "1.0.0",
  "private": true,
  "description": "Panduan fallback download video dengan yt-dlp, FFmpeg, cookies, dan opsi mobile.",
  "scripts": {
    "dev": "python3 -m http.server 3000",
    "build": "mkdir -p dist && cp -R index.html src dist/",
    "preview": "python3 -m http.server 3000 -d dist"
  }
}
